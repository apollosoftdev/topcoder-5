type: object
properties:
  status:
    type: string
    enum:
      - SUCCESS
      - FAIL
      - ALREADY_CALCULATED
    description: The result status of the calculation
  message:
    type: string
    description: A descriptive message about the result
